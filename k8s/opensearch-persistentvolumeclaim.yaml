apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    serviceName: opensearch-data
  name: opensearch-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
