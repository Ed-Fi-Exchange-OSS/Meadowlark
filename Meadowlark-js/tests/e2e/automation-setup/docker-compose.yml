version: '3'
services:

  db-e2e:
    container_name: postgres-meadowlark-e2e
    image: postgres:14.3-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=abcdefgh1!
    ports:
      # If no port provided through an env file, default to 5433
      - ${POSTGRES_PORT:-5433}:5432
    volumes:
      - meadowlark-pgsql-e2e:/var/lib/postgresql/data

  opensearch-node-e2e:
    image: opensearchproject/opensearch:latest
    container_name: opensearch-node-e2e
    environment:
      cluster.name: opensearch-cluster-e2e
      bootstrap.memory_lock: "true" # along with the memlock settings below, disables swapping
      OPENSEARCH_JAVA_OPTS: -Xms512m -Xmx512m # minimum and maximum Java heap size, recommend setting both to 50% of system RAM
      discovery.type: single-node
      DISABLE_INSTALL_DEMO_CONFIG: "true" # disables execution of install_demo_configuration.sh bundled with security plugin, which installs demo certificates and security configurations to OpenSearch
      DISABLE_SECURITY_PLUGIN: "true" # disables security plugin entirely in OpenSearch by setting plugins.security.disabled: true in opensearch.yml
    mem_limit: 2g
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536 # maximum number of open files for the OpenSearch user, set to at least 65536 on modern systems
        hard: 65536
    volumes:
      - opensearch-data-e2e:/usr/share/opensearch/data
    ports:
      # If no port provided through an env file, default to 8200
      - ${OPENSEARCH_PORT:-8200}:9200
    restart: unless-stopped

volumes:
  meadowlark-pgsql-e2e:
    driver: local
  opensearch-data-e2e:
